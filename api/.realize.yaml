settings:
  legacy:
    force: true
    interval: 0s
server:
  status: true
  open: true
  port: 8020
  host: localhost
schema:
- name: app
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
